# Core scientific packages
pip install numpy pandas scikit-learn

# Deep learning packages - PyTorch
pip install torch 

# Experiment tracking -  Weights & Biases for experiment tracking
pip install wandb  

# ESM (if using ESM models)
pip install fair-esm

# Optional but likely needed - For progress bars
pip install tqdm 